tests:
  - description: "A project with no source can run the plugin"
    configuration:
      tasks:
        - check
      options:
        - --stacktrace
    expectation:
      no_source:
        - cpdKotlinCheck
